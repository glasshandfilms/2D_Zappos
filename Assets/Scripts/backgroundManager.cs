using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class backgroundManager : MonoBehaviour
{

    [SerializeField] private iconsManager iconsManager;
    [SerializeField] private GameObject blue;
    [SerializeField] private GameObject green;
    [SerializeField] private GameObject orange;
    [SerializeField] private GameObject purple;
    [SerializeField] private GameObject teal;


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*(if(iconsManager. = 10)
        {
            blue.GetComponent<WavySprite>().waveSpeed = 0.8f;
            green.GetComponent<WavySprite>().waveSpeed = 0.8f;
            orange.GetComponent<WavySprite>().waveSpeed = 0.8f;
            purple.GetComponent<WavySprite>().waveSpeed = 0.8f;
            teal.GetComponent<WavySprite>().waveSpeed = 0.8f;
        }*/
    }
}
